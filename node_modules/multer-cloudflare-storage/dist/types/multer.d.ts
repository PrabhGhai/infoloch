import type { StorageEngine } from "multer";
import type { Request } from "express";
declare class CloudflareStorage implements StorageEngine {
    private accountID;
    private accountToken;
    private destURL;
    constructor(accountID: string, accountToken: string);
    _handleFile(_req: Request, file: Express.Multer.File, callback: (error: Error | null, info?: Partial<Express.Multer.File>) => void): void;
    _removeFile(_req: Request, file: Express.Multer.File, callback: (error: Error | null) => void): void;
}
export { CloudflareStorage };
export default CloudflareStorage;
